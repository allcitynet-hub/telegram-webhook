{
  "name": "telegram-webhook",
  "version": "1.0.0",
  "description": "Telegram webhook for notifications",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {},
  "devDependencies": {
    "vercel": "^latest"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
